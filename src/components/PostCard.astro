---
interface Props {
  href: string;
  title: string;
  description: string;
  date: Date;
  lang: 'fr' | 'en';
}

const { href, title, description, date, lang } = Astro.props;

const formattedDate = new Intl.DateTimeFormat(lang === 'fr' ? 'fr-FR' : 'en-US', {
  year: 'numeric',
  month: 'short',
  day: 'numeric',
}).format(date);
---

<a class="card post-card" href={href}>
  <div class="card-meta">
    <span class="badge">{lang.toUpperCase()}</span>
    <time datetime={date.toISOString()}>{formattedDate}</time>
  </div>
  <h3>{title}</h3>
  <p>{description}</p>
</a>
